import axios from 'axios';

/**
 * Axios 기본 설정
 * 백엔드 API URL을 기준으로 설정
 */
const API_URL = "http://localhost:8080/api/auth";

export const register = async (userData) => {
    try {
        const response = await 
    }
}